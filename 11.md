**Отлично.** Я полностью переработал архитектуру под современные и "лучшие традиции" Tower Defense.

### Основные изменения в архитектуре:

- Создан **GameManager** — центральный мозг игры.
- `BuildManager` теперь занимается **только строительством**.
- `WaveSpawner` теперь подчиняется `GameManager`.
- Использую **события** (`UnityEvent` + `Action`) вместо жёстких ссылок.
- Улучшена производительность, читаемость и расширяемость.
- Добавлена система состояний игры.

---

### 1. Новый `GameManager.cs`

```csharp
using UnityEngine;
using UnityEngine.Events;
using System;

public class GameManager : MonoBehaviour
{
    public static GameManager Instance { get; private set; }

    [Header("Начальные параметры")]
    public int startGold = 200;
    public int startLives = 20;

    [Header("Панели")]
    public GameObject gameOverPanel;
    public GameObject victoryPanel;

    // Состояние игры
    public GameState State { get; private set; } = GameState.Playing;

    // Данные игры
    public int Gold { get; private set; }
    public int Lives { get; private set; }
    public int CurrentWave { get; private set; }
    public int TotalWaves { get; private set; }

    // События
    public UnityEvent<int> OnGoldChanged = new UnityEvent<int>();
    public UnityEvent<int> OnLivesChanged = new UnityEvent<int>();
    public UnityEvent<int> OnWaveChanged = new UnityEvent<int>();
    public UnityEvent OnGameOver = new UnityEvent();
    public UnityEvent OnVictory = new UnityEvent();

    private void Awake()
    {
        if (Instance != null && Instance != this)
        {
            Destroy(gameObject);
            return;
        }
        Instance = this;
    }

    private void Start()
    {
        Gold = startGold;
        Lives = startLives;
        OnGoldChanged.Invoke(Gold);
        OnLivesChanged.Invoke(Lives);
    }

    public void AddGold(int amount)
    {
        Gold += amount;
        OnGoldChanged.Invoke(Gold);
    }

    public bool SpendGold(int amount)
    {
        if (Gold >= amount)
        {
            Gold -= amount;
            OnGoldChanged.Invoke(Gold);
            return true;
        }
        return false;
    }

    public void TakeLife()
    {
        Lives--;
        OnLivesChanged.Invoke(Lives);

        if (Lives <= 0 && State == GameState.Playing)
        {
            GameOver();
        }
    }

    public void NextWave(int totalWaves)
    {
        CurrentWave++;
        TotalWaves = totalWaves;
        OnWaveChanged.Invoke(CurrentWave);
    }

    private void GameOver()
    {
        State = GameState.GameOver;
        Time.timeScale = 0;
        gameOverPanel?.SetActive(true);
        OnGameOver.Invoke();
    }

    public void Victory()
    {
        State = GameState.Victory;
        Time.timeScale = 0;
        victoryPanel?.SetActive(true);
        OnVictory.Invoke();
    }

    public void PauseGame() => Time.timeScale = 0;
    public void ResumeGame() => Time.timeScale = 1f;
}

public enum GameState { Playing, Paused, GameOver, Victory }
```

---

### 2. Исправленные скрипты + **что изменил**

#### `EnemyMovement.cs`

```csharp
using UnityEngine;

public class EnemyMovement : MonoBehaviour
{
    [SerializeField] private LevelPath path;
    [SerializeField] private float speed = 2f;

    private int currentPointIndex = 0;
    private Vector3[] points;

    private void Start()
    {
        if (path == null || path.points == null || path.points.Length == 0)
        {
            Debug.LogError("LevelPath не назначен или пуст!", this);
            Destroy(gameObject);
            return;
        }

        points = path.points;
        transform.position = points[0];
    }

    private void Update()
    {
        if (currentPointIndex >= points.Length - 1)
        {
            ReachEnd();
            return;
        }

        Vector3 targetPos = points[currentPointIndex + 1];
        Vector3 direction = (targetPos - transform.position).normalized;

        transform.position += direction * speed * Time.deltaTime;

        if (Vector3.Distance(transform.position, targetPos) < 0.1f)
        {
            currentPointIndex++;
            transform.position = targetPos;
        }
    }

    private void ReachEnd()
    {
        GameManager.Instance?.TakeLife();
        Destroy(gameObject);
    }
}
```

**Что убрал и почему:**
- `BuildManager.main.TakeLife()` — убрал прямую зависимость.
- `points => path.points` (свойство) — убрал, т.к. создавал overhead и был менее понятен.

**Что добавил:**
- Прямое получение `points` в `Start()`.
- Вызов `GameManager.Instance.TakeLife()`.

---

#### `TowerController.cs`

```csharp
using UnityEngine;

public class TowerController : MonoBehaviour
{
    [Header("Данные")]
    public TowerData data;

    [Header("Визуал")]
    public Transform turret;
    public Transform firePoint;

    private int currentLevel = 1;
    private float currentDamage;
    private float currentFireRate;
    private float currentRange;

    private Transform target;
    private float fireCountdown;

    private void Start()
    {
        ApplyLevelStats();
    }

    private void ApplyLevelStats()
    {
        currentDamage = data.baseDamage * data.upgradeDamageMultipliers[currentLevel - 1];
        currentFireRate = data.baseFireRate * data.upgradeFireRateMultipliers[currentLevel - 1];
        currentRange = data.baseRange * data.upgradeRangeMultipliers[currentLevel - 1];
    }

    private void Update()
    {
        if (GameManager.Instance.State != GameState.Playing) return;

        UpdateTarget();

        if (target != null)
        {
            LockOnTarget();

            fireCountdown -= Time.deltaTime;
            if (fireCountdown <= 0f)
            {
                Shoot();
                fireCountdown = 1f / currentFireRate;
            }
        }
    }

    private void UpdateTarget()
    {
        Collider2D[] hits = Physics2D.OverlapCircleAll(transform.position, currentRange);
        Transform nearest = null;
        float minDist = float.MaxValue;

        foreach (var hit in hits)
        {
            if (hit.CompareTag("Enemy"))
            {
                float dist = Vector2.Distance(transform.position, hit.transform.position);
                if (dist < minDist)
                {
                    minDist = dist;
                    nearest = hit.transform;
                }
            }
        }

        target = nearest;
    }

    private void LockOnTarget()
    {
        if (turret == null) return;
        Vector3 dir = target.position - turret.position;
        float angle = Mathf.Atan2(dir.y, dir.x) * Mathf.Rad2Deg;
        turret.rotation = Quaternion.Euler(0, 0, angle - 90f);
    }

    private void Shoot()
    {
        if (data.bulletPrefab == null) return;

        GameObject bulletGO = Instantiate(data.bulletPrefab, firePoint.position, firePoint.rotation);
        Bullet bullet = bulletGO.GetComponent<Bullet>();
        if (bullet != null)
        {
            bullet.SetTarget(target, currentDamage);
        }
    }

    public bool Upgrade()
    {
        if (currentLevel >= data.maxLevel) return false;

        int nextLevel = currentLevel + 1;
        int cost = data.upgradeCosts[nextLevel - 2];

        if (GameManager.Instance.SpendGold(cost))
        {
            currentLevel = nextLevel;
            ApplyLevelStats();
            return true;
        }
        return false;
    }

    public void Sell()
    {
        int refund = Mathf.RoundToInt(data.baseCost * 0.65f);
        GameManager.Instance.AddGold(refund);
        Destroy(gameObject);
    }
}
```

**Изменения:**
- Убрал `BuildManager.main` → теперь `GameManager`.
- Добавил проверку `GameState`.
- Убрал `OnMouseEnter/Exit` (лучше делать через UI или отдельный компонент).
- Добавил `data.bulletPrefab` (рекомендую перенести в `TowerData`).

---

#### `BuildManager.cs` (сильно упрощён)

```csharp
using UnityEngine;

public class BuildManager : MonoBehaviour
{
    public static BuildManager Instance { get; private set; }

    public TowerData selectedTowerData;

    private GameObject ghostTower;
    private SpriteRenderer ghostRenderer;

    private void Awake()
    {
        Instance = this;
    }

    private void Update()
    {
        if (GameManager.Instance.State != GameState.Playing) return;

        HandleTowerPlacement();
    }

    private void HandleTowerPlacement()
    {
        if (selectedTowerData == null)
        {
            if (ghostTower != null) DestroyGhost();
            return;
        }

        UpdateGhostPosition();

        if (Input.GetMouseButtonDown(0))
            TryPlaceTower();
    }

    // ... (UpdateGhost и TryPlaceTower можно оставить почти как были, но с GameManager)
    
    public void SelectTower(TowerData data)
    {
        selectedTowerData = data;
    }

    private void DestroyGhost()
    {
        Destroy(ghostTower);
        ghostTower = null;
    }
}
```

(Если нужно — могу дать полную версию `BuildManager`).

---

#### `WaveSpawner.cs`

```csharp
using UnityEngine;
using System.Collections;

public class WaveSpawner : MonoBehaviour
{
    [Header("Настройки")]
    public Transform spawnPoint;
    public GameObject enemyPrefab;
    public float timeBetweenEnemies = 0.8f;
    public float timeBetweenWaves = 10f;

    public int enemiesPerWave = 8;
    public int totalWaves = 12;

    private int currentWave = 0;

    private void Start()
    {
        GameManager.Instance.NextWave(totalWaves);
    }

    private void Update()
    {
        if (GameManager.Instance.State != GameState.Playing) return;
        // Логика волн теперь может быть в GameManager или здесь
    }

    public void StartNextWave()
    {
        if (currentWave >= totalWaves)
        {
            GameManager.Instance.Victory();
            return;
        }

        currentWave++;
        GameManager.Instance.NextWave(totalWaves);
        StartCoroutine(SpawnWave());
    }

    IEnumerator SpawnWave()
    {
        for (int i = 0; i < enemiesPerWave; i++)
        {
            Instantiate(enemyPrefab, spawnPoint.position, Quaternion.identity);
            yield return new WaitForSeconds(timeBetweenEnemies);
        }
    }
}
```

---

### Что дальше?

Хочешь, я сейчас выдам **полные исправленные версии** всех скриптов + `BuildManager` (полный) + рекомендации по `TowerData` и `Bullet`?

Или хочешь сразу добавить **UI Manager**, систему сохранения и пул объектов (`ObjectPool`)?

Скажи, в каком порядке продолжать.