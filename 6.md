# День 6 — Здоровье врага и HP-бар над головой ❤️💚🩸

**Цель дня**  
Добавить слизню систему здоровья: максимум HP, получение урона, смерть при нуле.  
Плюс красивая полоска HP над головой, которая уменьшается и всегда смотрит на камеру.

**Что мы получим**  
- У каждого врага есть здоровье (по умолчанию 80)  
- Зелёная полоска над головой показывает текущее HP  
- При 0 HP враг исчезает (потом добавим эффекты и награду)  
- Полоска всегда повёрнута лицом к игроку

### Что уже должно быть
- Рабочий префаб Slime с движением (EnemyMovement)  
- LevelPath.asset с точками пути  
- Главная камера в сцене

### Шаг 1. Скрипт здоровья
Создайте скрипт **EnemyHealth.cs**  

Он будет:  
• хранить максимальное и текущее здоровье  
• принимать урон через публичный метод TakeDamage  
• возвращать нормализованное значение (0…1) для полоски  
• уничтожать объект при смерти

### Шаг 2. Прикрепление здоровья к префабу
1. Открыть Slime.prefab  
2. Выделить корневой объект Slime  
3. Добавить компонент EnemyHealth  
4. Установить Max Health = 80 (или любое удобное значение)

Сохранить префаб.

### Шаг 3. Создание HP-бара (World Space)
1. Внутри префаба Slime → правой кнопкой → UI → Canvas  
2. Переименовать в HealthBar  
3. Настроить Canvas:  
   • Render Mode → World Space  
   • Scale → 0.01 / 0.01 / 0.01  
   • Rect Width ≈ 100–140, Height ≈ 10–16  
4. Сдвинуть по Y на 1.0–1.5 (над головой)  
5. Внутри HealthBar → UI → Image → назвать Fill  
6. Настроить Fill:  
   • Цвет → ярко-зелёный  
   • Image Type → Filled  
   • Fill Method → Horizontal  
   • Fill Origin → Left  

**Рекомендация** — добавить под Fill ещё один Image (серый фон, тип Simple).

### Шаг 4. Скрипт управления полоской
Создайте скрипт **HealthBarUI.cs**  

Он будет:  
• находить компонент EnemyHealth у родителя  
• каждый кадр обновлять fillAmount зелёной полоски  
• поворачивать весь бар к главной камере

### Шаг 5. Подключение скрипта к бару
1. Выделить объект HealthBar в префабе  
2. Добавить компонент HealthBarUI  
3. Перетащить объект Fill в поле Fill Image  

Сохранить префаб.

### Шаг 6. Тест
1. Перетащить Slime.prefab на сцену  
2. Нажать Play → должна быть видна полная зелёная полоска над головой  
3. **Временный тест урона** (добавьте в Update скрипта EnemyMovement):  
   при нажатии пробела вызвать TakeDamage(20f) на компоненте EnemyHealth  
4. Нажимайте пробел → полоска должна уменьшаться  
5. При 0 HP слизень исчезает

**Чек-лист, если не работает**
- Path в EnemyMovement заполнен?  
- Max Health больше нуля?  
- Поле Fill Image в HealthBarUI назначено?  
- Scale Canvas точно 0.01?  
- Нет ошибок в консоли?

### Идеи для улучшения (добавьте позже)
• Менять цвет полоски: зелёный → жёлтый → красный  
• Добавить серый фон под полоской  
• Скрывать бар, когда здоровье 100%  
• Мигание при получении урона  
• Частицы или звук при смерти

### Задания на сегодня
1. Попробуйте Max Health = 40 / 120 / 250 → что выглядит лучше?  
2. Добавьте серый фон под Fill  
3. Сделайте полоску красной, когда HP < 30%  
4. ★ Бонус: спрячьте бар полностью, когда здоровье полное  
5. Убейте слизня 5–6 ударами пробела → всё работает?

**Поделитесь результатом**  
• Скриншот HP-бара в игре  
• Что понравилось / сломалось  
• Короткое видео (очень приветствуется!)

**День 6 завершён!**  
Теперь враг не просто ходит — он может умирать.  
Завтра будем делать башни, которые будут наносить этот урон.  
Ты круто идёшь вперёд! 🔥

```csharp
EnemyHealth.cs
using UnityEngine;
public class EnemyHealth : MonoBehaviour
{
[SerializeField] private float maxHealth = 80f;
private float currentHealth;
[SerializeField] private GameObject deathEffectPrefab;
private void Awake()
{
currentHealth = maxHealth;
}
public void TakeDamage(float amount)
{
currentHealth -= amount;
if (currentHealth <= 0)
{
Die();
}
}
private void Die()
{
if (deathEffectPrefab != null)
{
Instantiate(deathEffectPrefab, transform.position, Quaternion.identity);
}
Destroy(gameObject);
}
public float GetHealthNormalized() => currentHealth / maxHealth;
}
````


```chsarp
HealthBarUI.cs
using UnityEngine;
using UnityEngine.UI;
public class HealthBarUI : MonoBehaviour
{
[SerializeField] private Image fillImage;
private EnemyHealth health;
private void Awake()
{
health = GetComponentInParent<EnemyHealth>();
if (health == null)
{
Debug.LogWarning("EnemyHealth не найден у родителя!", this);
}
}
private void LateUpdate()
{
if (health == null || fillImage == null) return;
fillImage.fillAmount = health.GetHealthNormalized();
transform.rotation = Camera.main.transform.rotation;
}
}
```