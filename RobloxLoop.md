### Что такое циклы в Roblox (Lua)?

**Циклы** — это супер-магия в скриптах Roblox! 😎 Они позволяют **повторять один и тот же код много раз автоматически**, без того чтобы копировать его 100 раз. Представь: хочешь создать 20 красных кубиков? Вместо 20 строк — 3 строки в цикле!

В Roblox скрипты пишутся на **Luau** (улучшенный Lua). Есть **4 типа циклов**.

#### Таблица: Все циклы в Roblox (просто и ясно)

| Тип цикла       | Синтаксис                          | Когда использовать?                          | Пример (что выведет в Output) |
|-----------------|------------------------------------|----------------------------------------------|-------------------------------|
| **for** (численный) | `for i = 1, 5 do ... end`         | Знать **сколько раз** повторить (счётчик).  | 1 2 3 4 5                    |
| **for** (общий)    | `for k,v in pairs(таблица) do ... end` | Перебрать **список** (игроки, предметы).   | a=1 b=2                      |
| **while**         | `while условие do ... end`        | Повторять **пока правда** (неизвестно сколько). | 0 1 2 (пока <3)              |
| **repeat until**  | `repeat ... until условие`        | **Минимум 1 раз**, потом пока не правда.    | Count:0 Count:1 Count:2      |

**Важно:** Всегда используй `task.wait(0.1)` внутри, чтобы не лагало! `break` — выйти из цикла раньше.

#### 1. Цикл **for** (самый простой для новичков!)
Повторяет **точно N раз**. `i` — счётчик (меняется сам).

**Пример: Отсчёт от 10 до 0 (как ракета! 🚀)**
```lua
for count = 10, 0, -1 do  -- start=10, end=0, step=-1
    print(count)
    task.wait(1)  -- пауза 1 сек
end
```
**Вывод:** 10 → 9 → ... → 0.

**В слот-машине** (которую мы делали): `for i=1,20 do` — 20 раз меняли символы для анимации! 🎰

#### 2. Цикл **for** по списку (pairs/ipairs)
Для **таблиц** (списков).

**Пример:**
```lua
local fruits = {"🍒", "🍋", "⭐"}  -- таблица
for i, fruit in ipairs(fruits) do  -- ipairs для номеров
    print(i .. ": " .. fruit)
end
```
**Вывод:** 1: 🍒  2: 🍋  3: ⭐

#### 3. **while** — Пока условие правда
Проверяет **перед** повтором. Может быть бесконечным (`while true do`).

**Пример: Считаем до 3**
```lua
local n = 0
while n < 3 do  -- Пока n меньше 3
    print(n)
    n = n + 1  -- Увеличиваем!
end
```
**Вывод:** 0 1 2

**Бесконечный:** Меняй цвет Part'а навсегда!
```lua
local part = workspace.Part  -- Твой Part
while true do
    part.Color = Color3.fromRGB(255,0,0)  -- Красный
    task.wait(1)
    part.Color = Color3.fromRGB(0,255,0)  -- Зелёный
    task.wait(1)
end
```

#### 4. **repeat until** — Повтори, пока не правда
Проверяет **после** (минимум 1 раз!).

**Пример:**
```lua
local count = 0
repeat
    print("Count:", count)
    count = count + 1
until count >= 3  -- До 3
```
**Вывод:** Count:0  Count:1  Count:2

#### Туториал: Попробуй сам! (5 минут)
1. **New → Baseplate**.
2. **ServerScriptService** → **Insert Object** → **Script**. Назови **LoopTest**.
3. Вставь **любой пример выше**.
4. **F5** (Play) — смотри Output (View → Output).
5. **Создай 10 Part'ов в цикле:**
```lua
for i = 1, 10 do
    local part = Instance.new("Part")
    part.Size = Vector3.new(4,4,4)
    part.Position = Vector3.new(i*5, 5, 0)
    part.BrickColor = BrickColor.Random()
    part.Parent = workspace
end
```
**Результат:** 10 разноцветных кубиков в ряд! 🎉

