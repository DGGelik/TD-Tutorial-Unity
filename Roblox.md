### Туториал: Создаём простую слот-машину (однорукий бандит) в Roblox Studio

Привет! Мы сделаем **супер простую слот-машину** с 3 барабанами (символами вроде вишенки 🍒, лимона 🍋 и звёздочки ⭐). Нажмёшь кнопку "СПИН" — потратишь 10 монет, барабаны закрутятся, и если все 3 символа одинаковые — выиграешь 100 монет! Баланс начинается с 100.


#### Шаг 1: Открой Roblox Studio и подготовь место
1. Запусти **Roblox Studio** (бесплатно скачай, если нет).
2. Нажми **New** → **Baseplate** (пустая площадка).
3. В левой панели **Explorer** найди **StarterGui** (это GUI для всех игроков).
4. Правой кнопкой на **StarterGui** → **Insert Object** → **ScreenGui**.
5. Переименуй ScreenGui в **SlotMachineGui** (кликни 2 раза на имя).

**Готово!** ScreenGui — это экран, который видит каждый игрок.

#### Шаг 2: Создай главный фрейм (рамку для машины)
1. Правой кнопкой на **SlotMachineGui** → **Insert Object** → **Frame**.
2. Переименуй Frame в **MainFrame**.
3. Выдели MainFrame (кликни в Explorer), справа панель **Properties**:
   - **Size**: {0.5, 0}, {0.4, 0} (половина экрана по ширине, 40% по высоте).
   - **Position**: {0.25, 0}, {0.3, 0} (по центру экрана).
   - **BackgroundColor3**: Выбери яркий зелёный (кликни на цвет, RGB: 0, 255, 0) или любой любимый.
   - **BorderSizePixel**: 0 (убери рамку).
   - **BackgroundTransparency**: 0.1 (чуток прозрачный).

**Как выглядит:** Большая зелёная рамка по центру экрана.

#### Шаг 3: Добавь 3 слота (барабана) для символов
1. Правой кнопкой на **MainFrame** → **Insert Object** → **TextLabel**. Назови **Slot1**.
   - **Properties**:
     - Size: {0.25, 0}, {0.4, 0}
     - Position: {0.05, 0}, {0.1, 0}
     - BackgroundColor3: Белый (255,255,255)
     - Text: "?" (временно)
     - TextColor3: Чёрный
     - TextScaled: true (текст растянется)
     - Font: SourceSansBold (большой жирный)
     - BorderSizePixel: 0

2. Скопируй Slot1: Правой кнопкой на Slot1 → **Duplicate**.
3. Переименуй копию в **Slot2**, Position: {0.35, 0}, {0.1, 0}.
4. Duplicate ещё раз → **Slot3**, Position: {0.65, 0}, {0.1, 0}.

**Как выглядит:** Три белых квадрата в ряд с "?" внутри — это барабаны!

#### Шаг 4: Добавь кнопку "СПИН" и баланс
1. Правой кнопкой на **MainFrame** → **Insert Object** → **TextButton**. Назови **SpinButton**.
   - Size: {0.4, 0}, {0.15, 0}
   - Position: {0.3, 0}, {0.6, 0} (снизу по центру)
   - BackgroundColor3: Красный (255, 0, 0)
   - Text: "СПИН!"
   - TextColor3: Белый
   - TextScaled: true
   - Font: SourceSansBold
   - BorderSizePixel: 0

2. Правой кнопкой на **MainFrame** → **Insert Object** → **TextLabel**. Назови **BalanceLabel**.
   - Size: {0.8, 0}, {0.1, 0}
   - Position: {0.1, 0}, {0.02, 0} (сверху)
   - BackgroundTransparency: 1 (прозрачный)
   - Text: "Баланс: 100 💰"
   - TextColor3: Жёлтый (255, 255, 0)
   - TextScaled: true
   - Font: SourceSansBold

**Как выглядит:** Сверху "Баланс: 100 💰", снизу красная кнопка "СПИН!", слева три "?".

#### Шаг 5: Добавь скрипт (магия!)
1. Правой кнопкой на **SlotMachineGui** → **Insert Object** → **LocalScript**. Назови **SlotScript**.
2. Двойной клик на **SlotScript** — откроется код. **Удалить всё внутри** и вставь этот код целиком:

```lua
-- Простой однорукий бандит! Нажми СПИН и выиграй!
local player = game.Players.LocalPlayer
local gui = script.Parent
local main = gui.MainFrame
local slot1 = main.Slot1
slot2 = main.Slot2
slot3 = main.Slot3
local spinBtn = main.SpinButton  -- <-- Имя кнопки!
local balanceLbl = main.BalanceLabel  -- <-- Имя баланса!

local balance = 100  -- Наши монеты
balanceLbl.Text = "Баланс: " .. balance .. " 💰"

local symbols = {"🍒", "🍋", "🍊", "🍇", "7️⃣", "⭐"}  -- Символы (эмодзи!)

spinBtn.MouseButton1Click:Connect(function()
    if balance < 10 then  -- Мало монет?
        balanceLbl.Text = "Недостаточно монет! 💸"
        wait(2)
        balanceLbl.Text = "Баланс: " .. balance .. " 💰"
        return
    end
    
    balance = balance - 10  -- Ставка 10 монет
    balanceLbl.Text = "Крутим... 🎰 Баланс: " .. balance .. " 💰"
    
    -- Анимация: быстро меняем символы 20 раз!
    for i = 1, 20 do
        slot1.Text = symbols[math.random(1, #symbols)]
        slot2.Text = symbols[math.random(1, #symbols)]
        slot3.Text = symbols[math.random(1, #symbols)]
        wait(0.1)  -- Пауза 0.1 секунды
    end
    
    -- Финальные символы
    local s1 = symbols[math.random(1, #symbols)]
    local s2 = symbols[math.random(1, #symbols)]
    local s3 = symbols[math.random(1, #symbols)]
    
    slot1.Text = s1
    slot2.Text = s2
    slot3.Text = s3
    
    -- Проверяем выигрыш!
    if s1 == s2 and s2 == s3 then
        balance = balance + 100  -- Выигрыш 100 монет!
        balanceLbl.Text = "🎉 ВЫИГРЫШ! +100 💰 Новый баланс: " .. balance .. " 💰"
        wait(3)
    else
        balanceLbl.Text = "Не повезло... 😔 Баланс: " .. balance .. " 💰"
        wait(2)
    end
    balanceLbl.Text = "Баланс: " .. balance .. " 💰"  -- Обновляем
end)
```

**Важно:** Если имена в Explorer другие — подправь в коде (строки с `slot1 = main.Slot1` и т.д.).

#### Шаг 6: Тестируем!
1. Нажми **F5** (Play) — войди в игру как игрок.
2. Увидишь слот-машину по центру!
3. Кликни "СПИН!" — закрутится, потратит 10 монет. Если три одинаковых — +100!
4. Нажми **Stop** (квадрат), сохрани: **File → Publish to Roblox** (создай игру бесплатно).

#### Шаг 7: Улучшения (если хочешь круче, но не обязательно)
- **Звуки:** Добавь Sound в MainFrame, в коде `sound:Play()` после спина.
- **Больше символов:** Добавь в `symbols = { ... , "💎", "🔔" }`.
- **Анимация лучше:** Замени `wait(0.1)` на TweenService (гугли "Roblox TweenService tutorial" — просто!).
- **Сохрани баланс:** Добавь DataStoreService (для профи).

**Готово!** Ты сделал свою первую слот-машину! 🎰 Поделись с друзьями. Если ошибка — скопируй текст ошибки и спроси. Удачи, геймдевелопер! 🚀




### Туториал: Создаём простую викторину в Roblox Studio

Привет! Мы сделаем **супер простую викторину** с 5 вопросами (о животных, еде и Roblox). Игрок выбирает ответ из 4 кнопок, если правильно — +10 очков! В конце покажет итог. Всё на экране, как в игре. Баланс начинается с 0.


#### Шаг 1: Открой Roblox Studio и подготовь
1. Запусти **Roblox Studio**.
2. **New** → **Baseplate**.
3. В **Explorer** (слева): Правой кнопкой на **StarterGui** → **Insert Object** → **ScreenGui**. Назови **QuizGui**.

#### Шаг 2: Главная рамка (фрейм)
1. Правой кнопкой на **QuizGui** → **Insert Object** → **Frame**. Назови **MainFrame**.
2. Выдели **MainFrame**, в **Properties** (справа):
   - **Size**: {0.6, 0}, {0.7, 0} (большая рамка).
   - **Position**: {0.2, 0}, {0.15, 0} (по центру).
   - **BackgroundColor3**: Синий (0, 100, 255).
   - **BorderSizePixel**: 0.
   - **BackgroundTransparency**: 0.2 (чуток прозрачный).

#### Шаг 3: Вопрос и счёт
1. Правой кнопкой на **MainFrame** → **TextLabel**. Назови **QuestionLabel**.
   - **Size**: {0.9, 0}, {0.3, 0}
   - **Position**: {0.05, 0}, {0.05, 0}
   - **BackgroundTransparency**: 1
   - **Text**: "Готов к викторине? 😊"
   - **TextColor3**: Белый
   - **TextScaled**: true
   - **Font**: SourceSansBold

2. Правой кнопкой на **MainFrame** → **TextLabel**. Назови **ScoreLabel**.
   - **Size**: {0.9, 0}, {0.1, 0}
   - **Position**: {0.05, 0}, {0.85, 0} (сверху)
   - **BackgroundTransparency**: 1
   - **Text**: "Очки: 0 ⭐"
   - **TextColor3**: Жёлтый (255, 255, 0)
   - **TextScaled**: true
   - **Font**: SourceSansBold

#### Шаг 4: 4 кнопки ответов
1. Правой кнопкой на **MainFrame** → **TextButton**. Назови **Answer1**.
   - **Size**: {0.4, 0}, {0.15, 0}
   - **Position**: {0.05, 0}, {0.4, 0}
   - **BackgroundColor3**: Зелёный (0, 255, 0)
   - **Text**: "Ответ 1"
   - **TextColor3**: Белый
   - **TextScaled**: true
   - **Font**: SourceSans

2. **Duplicate** Answer1 → **Answer2**, Position: {0.55, 0}, {0.4, 0}, BackgroundColor3: Оранжевый (255, 165, 0).

3. **Duplicate** → **Answer3**, Position: {0.05, 0}, {0.6, 0}, BackgroundColor3: Красный (255, 0, 0).

4. **Duplicate** → **Answer4**, Position: {0.55, 0}, {0.6, 0}, BackgroundColor3: Синий (0, 0, 255).

#### Шаг 5: Кнопка "Следующий вопрос"
1. Правой кнопкой на **MainFrame** → **TextButton**. Назови **NextButton**.
   - **Size**: {0.3, 0}, {0.12, 0}
   - **Position**: {0.35, 0}, {0.8, 0}
   - **BackgroundColor3**: Фиолетовый (128, 0, 128)
   - **Text**: "Следующий ➡️"
   - **TextColor3**: Белый
   - **TextScaled**: true
   - **Font**: SourceSansBold
   - **Visible**: false (сначала спрячь в Properties).

#### Шаг 6: Добавь скрипт (всё оживает!)
1. Правой кнопкой на **QuizGui** → **Insert Object** → **LocalScript**. Назови **QuizScript**.
2. Двойной клик на **QuizScript** — удали всё и вставь этот код **целиком**:

```lua
-- Простая викторина! 5 вопросов, +10 очков за правильный!
local player = game.Players.LocalPlayer
local gui = script.Parent
local main = gui.MainFrame
local question = main.QuestionLabel
local scoreLbl = main.ScoreLabel
local ans1 = main.Answer1
local ans2 = main.Answer2
local ans3 = main.Answer3
local ans4 = main.Answer4
local nextBtn = main.NextButton

local score = 0  -- Очки
local currentQuestion = 1  -- Текущий вопрос
scoreLbl.Text = "Очки: " .. score .. " ⭐"

-- Вопросы: {вопрос, {ответ1, ответ2, правильный, ответ4}}
local questions = {
    {"Сколько лап у кошки?", {"2", "4", "4", "6"}},
    {"Какой фрукт жёлтый?", {"Яблоко", "Банан", "Банан", "Апельсин"}},
    {"Что такое Roblox?", {"Игра", "Игра", "Еда", "Машина"}},
    {"Цвет неба?", {"Красный", "Зелёный", "Синий", "Синий"}},
    {"Сколько в дне часов?", {"12", "24", "24", "36"}}
}

-- Функция показать вопрос
local function showQuestion(qNum)
    local q = questions[qNum]
    question.Text = q[1]
    ans1.Text = q[2][1]
    ans2.Text = q[2][2]
    ans3.Text = q[2][3]
    ans4.Text = q[2][4]
    
    -- Сброс цветов кнопок
    ans1.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
    ans2.BackgroundColor3 = Color3.fromRGB(255, 165, 0)
    ans3.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    ans4.BackgroundColor3 = Color3.fromRGB(0, 0, 255)
    
    nextBtn.Visible = false
end

-- Клик по ответу
local function checkAnswer(correct)
    if correct then
        score = score + 10
        scoreLbl.Text = "✅ Правильно! Очки: " .. score .. " ⭐"
    else
        scoreLbl.Text = "❌ Неправильно! Очки: " .. score .. " ⭐"
    end
    nextBtn.Visible = true  -- Покажи "Следующий"
end

-- Подключаем кнопки ответов
ans1.MouseButton1Click:Connect(function() checkAnswer(questions[currentQuestion][2][1] == questions[currentQuestion][2][3]) end)
ans2.MouseButton1Click:Connect(function() checkAnswer(questions[currentQuestion][2][2] == questions[currentQuestion][2][3]) end)
ans3.MouseButton1Click:Connect(function() checkAnswer(questions[currentQuestion][2][3] == questions[currentQuestion][2][3]) end)
ans4.MouseButton1Click:Connect(function() checkAnswer(questions[currentQuestion][2][4] == questions[currentQuestion][2][3]) end)

-- Следующий вопрос
nextBtn.MouseButton1Click:Connect(function()
    currentQuestion = currentQuestion + 1
    if currentQuestion > #questions then
        question.Text = "Викторина окончена! 🎉 Итог: " .. score .. "/50 очков!"
        nextBtn.Text = "Играть заново!"
        nextBtn.MouseButton1Click:Connect(function()
            score = 0
            currentQuestion = 1
            scoreLbl.Text = "Очки: 0 ⭐"
            showQuestion(1)
            nextBtn.Text = "Следующий ➡️"
            nextBtn.MouseButton1Click:Connect(nextBtn.MouseButton1Click)  -- Верни старую функцию
        end)
    else
        showQuestion(currentQuestion)
    end
end)

-- Первый вопрос
showQuestion(1)
```

**Важно:** Если имена в Explorer другие — измени в коде (например, `ans1 = main.Answer1`).

#### Шаг 7: Тестируем!
1. **F5** (Play) — увидишь викторину!
2. Выбери ответ → "Следующий" → следующий вопрос.
3. В конце "Играть заново!".
4. **Stop**, сохрани: **File → Publish to Roblox**.

#### Улучшения (если хочешь):
- **Больше вопросов:** Добавь в `questions = { ... }`.
- **Звуки:** Добавь Sound, `sound:Play()` при правильном.
- **Таймер:** Добавь `wait(1)` или ProximityPrompt.
- **Сохранение очков:** DataStoreService (погугли туториал).

Готово! Ты сделал викторину! ⭐ Поделись с друзьями. Если баг — скажи текст ошибки. Удачи! 🚀