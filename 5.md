# День 5 — Враг начинает двигаться по дороге! 🐌🛤️

**Цель дня**  
Научить слизня (Slime) ходить по точкам пути от WP0 до WP15 и исчезать в конце — как в классических tower defense.

**Что получится в конце дня**  
Перетаскиваешь префаб Slime на сцену → нажимаешь Play → слизень бежит по всей дороге и пропадает после последней точки.  
Это уже почти настоящая игра!

### Что должно быть готово

- ScriptableObject `LevelPath.asset` с массивом точек  
- Точки WP0 … WP15 расставлены по центру дороги  
- Префаб `Slime.prefab`

---

### Шаг 1. Создаём скрипт

1. В Project → правой кнопкой → **Create → C# Script**  
2. Назови **EnemyMovement** (без пробелов, без .cs в названии)  
3. Открой файл двойным кликом

```csharp
using UnityEngine;

public class EnemyMovement : MonoBehaviour
{
    public LevelPath path;           // ← сюда перетаскиваем LevelPath.asset
    public float speed = 2f;

    private int currentPointIndex = 0;

    void Start()
    {
        if (path == null || path.points == null || path.points.Length == 0)
        {
            Debug.LogError($"У {gameObject.name} нет пути! Перетащите LevelPath в поле Path.", gameObject);
            enabled = false;
            return;
        }

        // ставим на первую точку
        transform.position = path.points[0].position;
    }

    void Update()
    {
        // дошли до конца?
        if (currentPointIndex >= path.points.Length - 1)
        {
            Destroy(gameObject);
            return;
        }

        // следующая точка
        Vector3 nextPoint = path.points[currentPointIndex + 1].position;

        // плавное движение к точке
        transform.position = Vector3.MoveTowards(
            transform.position,
            nextPoint,
            speed * Time.deltaTime
        );

        // достаточно близко → переходим дальше
        if (Vector3.Distance(transform.position, nextPoint) < 0.05f)
        {
            currentPointIndex++;
        }
    }
}
```
---

### Шаг 2. Краткое объяснение, что будет делать скрипт

- Поле для пути (LevelPath)  
- Поле для скорости (можно менять в инспекторе)  
- При появлении врага — проверка пути + установка на WP0  
- Каждый кадр — плавное движение к следующей точке  
- Когда дошли до последней — враг уничтожается

---

### Шаг 3. Прикрепляем скрипт к префабу

1. Дважды кликни по `Assets/Prefabs/Enemies/Slime.prefab`  
2. Выдели корневой объект **Slime** внутри префаба  
3. Inspector → **Add Component** → найди `EnemyMovement` → добавь  
4. В поле **Path** перетащи `LevelPath.asset`

Теперь все экземпляры этого префаба будут автоматически знать дорогу.

---

### Шаг 4. Тест-драйв

1. Перетащи `Slime.prefab` на сцену  
2. Нажми **Play**  
3. Ожидаемый результат:

   • появляется на WP0  
   • плавно идёт по всем точкам  
   • исчезает после WP15

**Не двигается? Быстрый чек-лист**

- Поле Path заполнено?  
- В консоли ошибка «нет пути»?  
- Точки стоят ровно по дороге?  
- Компонент включён (галочка)?  
- Массив points в LevelPath не пустой?

---

### Варианты движения (для будущего)

| Вариант               | Плавность | Поворот лицом | Сложность | Когда лучше использовать          |
|-----------------------|-----------|---------------|-----------|------------------------------------|
| Направление + Distance| ★★☆       | Нет           | ★☆☆       | Самые первые уроки                 |
| MoveTowards (рекомендую сейчас) | ★★★★      | Нет           | ★★☆       | Стандарт в большинстве TD-игр      |
| MoveTowards + поворот | ★★★★★     | Да            | ★★★       | Когда хочется живой анимации       |

---

### Задания на сегодня (выбери 2–4 любых)

1. Поменяй **speed** в инспекторе (попробуй 0.5 / 3 / 8 / 12)  
   → запиши, как изменилось поведение

2. Добавь точку **WP16** дальше конца пути  
   → увеличь массив в LevelPath до 17  
   → проверь, дошёл ли враг до неё

3. Сделай, чтобы враг **не исчезал**, а останавливался в конце  
   → подсказка: вместо одной строчки можно написать `enabled = false;`

4. ★ Бонус — добавь поворот лицом к движению  
   Попробуй разные углы поворота (-90 / 0 / 90 / -180)  
   При каком спрайт смотрит правильно?

Напиши в комментариях:  
- какой speed тебе понравился больше всего  
- что получилось из заданий  
- скрин или видео — будет очень интересно! 📸

**День 5 завершён!**  
У нас есть враг, который ходит по дороге — это уже огромный прогресс.  
Завтра будем делать волны и спавнер.  
Молодец! 🔥


