### День 7 — Wave Spawner (базовая структура)

**Цель дня:** Сделать систему, которая будет автоматически выпускать волны врагов.

1. В Hierarchy → правой кнопкой → Create Empty → назовите **GameManager**

**Пояснение:**  
GameManager — это специальный объект, который будет управлять всей игрой (волны, золото, счёт и т.д.).  
Мы создаём его сейчас, чтобы в будущем сюда можно было прикреплять все управляющие скрипты.  
Если не создать GameManager, то позже будет сложно найти, где хранятся глобальные настройки.

2. Assets → Create → C# Script → назовите `WaveSpawner`

**Пояснение:**  
WaveSpawner — это скрипт, который будет отвечать за спавн врагов волнами.  
Мы создаём его отдельно, чтобы потом можно было легко менять количество врагов, время между волнами и т.д.

3. Откройте скрипт и вставьте следующий код:

```csharp
using UnityEngine;
using System.Collections;

public class WaveSpawner : MonoBehaviour
{
    [Header("Настройки спавна")]
    public Transform spawnPoint;                // WP0
    public GameObject enemyPrefab;              // EnemySlime.prefab
    public float timeBetweenEnemies = 0.8f;     // пауза между врагами в волне
    public float timeBetweenWaves = 8f;         // пауза между волнами

    [Header("Тестовые волны")]
    public int enemiesInWave = 5;
    public int totalWavesToSpawn = 3;           // пока только 3 волны для теста

    private int currentWave = 0;
    private float countdown = 2f;               // время до первой волны

    private void Update()
    {
        if (countdown <= 0f)
        {
            if (currentWave < totalWavesToSpawn)
            {
                StartCoroutine(SpawnWave());
                currentWave++;
                countdown = timeBetweenWaves;
            }
        }
        else
        {
            countdown -= Time.deltaTime;
        }
    }

    IEnumerator SpawnWave()
    {
        for (int i = 0; i < enemiesInWave; i++)
        {
            SpawnEnemy();
            yield return new WaitForSeconds(timeBetweenEnemies);
        }
    }

    void SpawnEnemy()
    {
        Instantiate(enemyPrefab, spawnPoint.position, Quaternion.identity);
    }
}
```
**Пояснение к коду**

- `spawnPoint` — место, откуда появляются враги (обычно **WP0**).
- `enemyPrefab` — какой враг будет спавниться.
- `timeBetweenEnemies` и `timeBetweenWaves` — паузы между врагами и волнами.
- `StartCoroutine` — позволяет делать паузы между спавном врагов внутри одной волны.

---

### 4. Прикрепите скрипт к объекту GameManager

**Пояснение:**  
Мы прикрепляем скрипт именно к **GameManager**, потому что он будет управлять всей логикой игры. Это удобно — все важные скрипты находятся в одном месте.

---

### 5. В инспекторе заполните поля:

- **Spawn Point** → перетащите **WP0** из Waypoints  
- **Enemy Prefab** → перетащите **EnemySlime.prefab**  
- **Enemies In Wave** → `5`  
- **Total Waves To Spawn** → `3`

**Пояснение:**  
Эти поля говорят скрипту: «Откуда спавнить», «Кого спавнить» и «Сколько».  
Если не заполнить — враги не появятся или будут спавниться в неправильном месте.

---

### 6. Создайте Canvas (если ещё нет)

1. **Hierarchy** → **UI** → **Canvas**  
2. Переименуйте в **UICanvas**  
3. Добавьте дочерний объект: **UI** → **Text - TextMeshPro**  
   (если TMP не установлен — установите через **Package Manager** → **TextMeshPro**)

**Пояснение:**  
Canvas — это специальный слой для всего интерфейса (волны, золото, здоровье).  
**TextMeshPro** — современный текст в Unity, он красивее и удобнее обычного `Text`.

---

### 7. Настройте текст

- Выберите **TextMeshPro - UGUI**  
- В поле **Text** напишите: `Wave Incoming: --`

---

### ✅ Контрольная проверка дня 7

Нажмите **Play** → через 2 секунды должны начать появляться **5 слизней** с интервалом **0.8 сек**, потом пауза **8 секунд**, потом снова волна — всего **3 раза**.  

Всё работает — поздравляем! 🎉
---

### День 8 — Улучшение WaveSpawner + countdown UI

**Цель дня:** Сделать видимый обратный отсчёт до следующей волны.

1. Откройте WaveSpawner.cs и добавьте следующие поля и логику:

```csharp
// добавьте в начало класса
[Header("UI")]
public TMPro.TextMeshProUGUI countdownText;

// замените Update на:
private void Update()
{
    if (countdown <= 0f)
    {
        if (currentWave < totalWavesToSpawn)
        {
            StartCoroutine(SpawnWave());
            currentWave++;
            countdown = timeBetweenWaves;
        }
    }
    else
    {
        countdown -= Time.deltaTime;
        if (countdownText != null)
        {
            countdownText.text = $"Следующая волна через: {Mathf.Ceil(countdown)}";
        }
    }
}
```
Пояснение:
Мы добавляем текст, который показывает игроку, сколько секунд осталось до следующей волны. Это важно для геймплея — игрок должен видеть, когда готовиться к новой волне.


2. В Hierarchy → UICanvas → создайте новый TextMeshPro - Text (назовите CountdownText)
   - Rect Transform → Anchor → Top Center
   - Pos Y ≈ -80
   - Font Size → 48
   - Alignment → Center
Пояснение:
Мы размещаем текст вверху экрана, чтобы игроку было удобно смотреть.

3. В инспекторе WaveSpawner → перетащите CountdownText в поле Countdown Text

**Контрольная проверка дня 8**  
Теперь перед каждой волной видно «Следующая волна через: 8» → 7 → … → 1 → волна начинается.

---
# День 9 — Archer Tower (префаб + визуал)

**Цель дня:** Создать готовый префаб башни-лучника, чтобы потом можно было легко ставить её на карту.

---

## Пошаговая инструкция (максимально подробно)

### 1. Создаём пустой объект ArcherTower

1. Убедись, что у тебя открыто окно **Hierarchy** (обычно справа).
2. Наведи мышку на **любое пустое место** внутри окна Hierarchy (не на существующий объект!).
3. Нажми **правую кнопку мыши**.
4. В меню выбери **Create Empty**.
5. Появится объект `GameObject` в самом низу списка.
6. Кликни на него **один раз** (чтобы выделить).
7. Нажми клавишу **F2** (или медленно кликни на название ещё раз).
8. Напиши `ArcherTower` и нажми **Enter**.

### 2. Добавляем Sprite Renderer (основание башни)

1. В окне **Hierarchy** кликни на объект **ArcherTower** (он должен выделиться синим).
2. Посмотри в окно **Inspector** (справа).
3. В самом низу Inspector нажми большую синюю кнопку **Add Component**.
4. В поисковой строке начни печатать `Sprite Renderer`.
5. Когда появится компонент, кликни на него левой кнопкой мыши.
6. В Inspector появится поле **Sprite**.
7. Открой окно **Project** (внизу экрана).
8. Найди спрайт башни из Kenney (обычно `Assets/Kenney/Sprites/Towers` или похожая папка).
9. Перетащи нужный спрайт (с основанием/ножкой) мышкой прямо в поле **Sprite** в Inspector.

### 3. Создаём дочерний объект Turret (вращающаяся часть)

1. В **Hierarchy** кликни **правой кнопкой мыши** прямо на объекте **ArcherTower**.
2. Выбери **Create Empty**.
3. Новый объект появится **внутри** ArcherTower (с отступом вправо).
4. Переименуй его в `Turret` (F2 или двойной клик).
5. Выдели объект **Turret**.
6. Нажми **Add Component** → найди и добавь `Sprite Renderer`.
7. Перетащи в поле **Sprite** спрайт ствола/головы башни (если есть отдельный в Kenney).

### 4. Добавляем зону обнаружения (Circle Collider 2D)

1. В Hierarchy выдели **ArcherTower** (не Turret!).
2. В Inspector нажми **Add Component**.
3. Напиши в поиске `Circle Collider 2D` и добавь его.
4. Поставь галочку **Is Trigger** (обязательно!).
5. В поле **Radius** поставь значение `3.5`.

**Пояснение:**  
Circle Collider с Is Trigger нужен, чтобы башня «видела» врагов в радиусе 3.5 единиц и могла по ним стрелять.

---

### 5. Сохраняем как префаб

1. В окне **Project** открой папку:  
   `Assets → Prefabs → Towers`  
   (если папки `Towers` нет — создай: правый клик внутри Prefabs → **Create** → **Folder** → назови `Towers`).
2. В Hierarchy выдели объект **ArcherTower**.
3. Зажми левую кнопку мыши на **ArcherTower** и перетащи его в папку `Towers` в окне Project.
4. Отпусти кнопку — появится синий файл **ArcherTower.prefab**.

### 6. Удаляем объект из сцены

1. В Hierarchy кликни на **ArcherTower**.
2. Нажми клавишу **Delete** (или правый клик → Delete).

---

## ✅ Результат дня 9

В папке `Assets/Prefabs/Towers` лежит готовый префаб **ArcherTower.prefab** с:
- основанием (Sprite Renderer)
- вращающейся частью `Turret`
- зоной обнаружения 3.5

Готово! Башня полностью визуально настроена и сохранена как префаб.

---

**Следующий шаг:** День 10 — скрипт для башни (стрельба и поворот).
---
# День 10 — TowerController (поиск цели + поворот)

**Цель дня:** Добавить скрипт башне, чтобы она **находила ближайшего врага** в радиусе 3.5, **поворачивалась к нему** и **готовилась стрелять** (пока без снарядов).

---

## Пошаговая инструкция (максимально подробно)

### 0. Важно: Проверьте тег у врагов (обязательно!)

1. В окне **Project** найди папку `Assets/Prefabs/Enemies` (или где лежит EnemySlime).
2. Двойной клик на **EnemySlime.prefab** (откроется префаб в режиме редактирования).
3. В окне **Hierarchy** (теперь показывает префаб) кликни на **EnemySlime**.
4. В **Inspector** в самом **вверху** найди поле **Tag** (рядом с именем объекта).
5. Если там **Untagged** — кликни на выпадающий список **Tag**.
6. Если **Enemy** нет в списке — кликни **Add Tag...** → **+** → напиши `Enemy` → **Save**.
7. Выбери **Enemy** из списка и нажми **Enter**.
8. Нажми **Save** вверху (или Ctrl+S).
9. Вернись в основную сцену (кликни на вкладку **Scene**).

**Пояснение:** Без тега `Enemy` башня не увидит слизней!

---

### 1. Создайте скрипт TowerController.cs

1. В окне **Project** найди папку `Assets/Scripts` (если нет — создай: правый клик → **Create** → **Folder** → `Scripts`).
2. Наведи мышку на папку **Scripts**, нажми **правую кнопку мыши**.
3. Выбери **Create** → **C# Script**.
4. Новый файл называется `NewScript` — кликни на него **один раз**, нажми **F2**.
5. Переименуй в `TowerController` и нажми **Enter**.
6. **Двойной клик** на файле `TowerController.cs` — откроется в Visual Studio (или другом редакторе).

### 2. Вставьте код скрипта

**Полностью замените** весь код в файле на этот:

```csharp
using UnityEngine;

public class TowerController : MonoBehaviour
{
    [Header("Настройки")]
    public float range = 3.5f;
    public float fireRate = 1.2f;       // выстрелы в секунду
    public float damage = 25f;

    [Header("Компоненты")]
    public Transform turret;            // дочерний объект Turret
    public Transform firePoint;         // откуда вылетает снаряд (позже)

    private Transform target;
    private float fireCountdown = 0f;

    private void Update()
    {
        UpdateTarget();
        if (target != null)
        {
            LockOnTarget();
            if (fireCountdown <= 0f)
            {
                Shoot();
                fireCountdown = 1f / fireRate;
            }
        }

        fireCountdown -= Time.deltaTime;
    }

    void UpdateTarget()
    {
        GameObject[] enemies = GameObject.FindGameObjectsWithTag("Enemy");
        float shortestDistance = Mathf.Infinity;
        Transform nearestEnemy = null;

        foreach (GameObject enemy in enemies)
        {
            float distanceToEnemy = Vector2.Distance(transform.position, enemy.transform.position);
            if (distanceToEnemy < shortestDistance && distanceToEnemy <= range)
            {
                shortestDistance = distanceToEnemy;
                nearestEnemy = enemy.transform;
            }
        }

        target = nearestEnemy;
    }

    void LockOnTarget()
    {
        if (turret == null) return;

        Vector3 dir = target.position - turret.position;
        float angle = Mathf.Atan2(dir.y, dir.x) * Mathf.Rad2Deg;
        turret.rotation = Quaternion.Euler(0f, 0f, angle - 90f); // -90 для спрайтов, смотрящих вверх
    }

    void Shoot()
    {
        Debug.Log("Выстрел!");
        // Пока просто лог — снаряд добавим завтра
    }
}
```

1. Прикрепите скрипт к префабу ArcherTower
2. Создайте пустой дочерний объект **FirePoint** внутри Turret (позиция на конце ствола)
3. Перетащите Turret и FirePoint в соответствующие поля инспектора

**Контрольная проверка дня 10**  
Поставьте башню в сцену → запустите → подвиньте врага в радиус → башня должна поворачиваться к нему.

---

### День 11 — Снаряд (Bullet)

1. Hierarchy → Create Empty → **Bullet**

2. Добавьте Sprite Renderer → маленький круг или стрелка

3. Добавьте **Rigidbody 2D**
   - Body Type = Kinematic
   - Gravity Scale = 0

4. Добавьте **Circle Collider 2D** → Is Trigger = true → Radius ≈ 0.15

5. Создайте скрипт **Bullet.cs**

```csharp
using UnityEngine;

public class Bullet : MonoBehaviour
{
    public float speed = 10f;
    public float damage = 25f;
    public Transform target;

    private void Update()
    {
        if (target == null)
        {
            Destroy(gameObject);
            return;
        }

        Vector2 direction = (target.position - transform.position).normalized;
        transform.position += (Vector3)direction * speed * Time.deltaTime;

        // Поворот спрайта по направлению (если нужно)
        float angle = Mathf.Atan2(direction.y, direction.x) * Mathf.Rad2Deg;
        transform.rotation = Quaternion.Euler(0, 0, angle - 90f);
    }

    private void OnTriggerEnter2D(Collider2D other)
    {
        if (other.CompareTag("Enemy"))
        {
            EnemyHealth enemy = other.GetComponent<EnemyHealth>();
            if (enemy != null)
            {
                enemy.TakeDamage(damage);
            }
            Destroy(gameObject);
        }
    }
}
```

6. Прикрепите скрипт к префабу Bullet → сохраните в Prefabs/Projectiles/Bullet.prefab

7. В TowerController добавьте поле и измените Shoot():

```csharp
public GameObject bulletPrefab;

void Shoot()
{
    GameObject bulletGO = Instantiate(bulletPrefab, firePoint.position, firePoint.rotation);
    Bullet bullet = bulletGO.GetComponent<Bullet>();
    if (bullet != null)
    {
        bullet.target = target;
        bullet.damage = damage;
    }
}
```

8. В инспекторе ArcherTower → перетащите Bullet.prefab в поле Bullet Prefab

**Контрольная проверка дня 11**  
Башня теперь должна выпускать снаряды, которые летят за врагом и наносят урон (HP-бар уменьшается).

---

### День 12 — Простое размещение башен

Создайте скрипт **BuildManager.cs**

```csharp
using UnityEngine;

public class BuildManager : MonoBehaviour
{
    public static BuildManager main;

    public GameObject towerPrefab;
    public int gold = 200;

    private void Awake()
    {
        main = this;
    }

    private void Update()
    {
        if (Input.GetMouseButtonDown(0))
        {
            Vector2 mousePos = Camera.main.ScreenToWorldPoint(Input.mousePosition);
            PlaceTower(mousePos);
        }
    }

    void PlaceTower(Vector2 position)
    {
        // Пока простая проверка — позже добавим Tilemap-слой
        if (gold >= 80)
        {
            Instantiate(towerPrefab, position, Quaternion.identity);
            gold -= 80;
            Debug.Log("Башня построена. Остаток золота: " + gold);
        }
    }
}
```

1. Прикрепите BuildManager к GameManager
2. Перетащите ArcherTower.prefab в поле Tower Prefab

**Контрольная проверка дня 12**  
Нажмите Play → кликайте левой кнопкой мыши по карте → должны появляться башни (пока без проверки на траву/дорогу).

---

**Итог Месяца 1**  
К концу 12-го дня у вас должен быть рабочий мини-прототип:
- враги идут по пути волнами
- башни можно ставить кликом
- башни поворачиваются, стреляют, наносят урон
- враги теряют здоровье и умирают
